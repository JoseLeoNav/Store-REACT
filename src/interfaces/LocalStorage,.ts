export default interface LocalStorage {
  colors: string[];
  description: string;
  id: string;
  images: string[];
  onsale: boolean;
  price: number;
  stock: number;
  title: string;
  units: number;
}
